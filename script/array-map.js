const number = [1, 2, 3, 4, 5, 6, 7, 9];

// 👉mailtiplay:
const newMalti = number.map((element) => {
  return element * element;
});
// console.log(newMalti);

// 👉other again
const friendName = ["roy", "rocky", "function", "alom"];
const newList = friendName.map((value, index) => {
  // console.log(value);
  // console.log(index);
});

// 👉const newNumber = number.map((index) => index + 1);
// 👉const newNumber = number.map((index) => {
//   return index;
// });
// console.log(newNumber);

// 👉basic type fo: loop -ing
// let tem = [];
// for (let index = 0; index < number.length; index++) {
//   const element = number[index];
//   const sum = element + 1;
//   tem.push(sum);
// }
// console.log(tem);
